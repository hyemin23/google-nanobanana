
import { PosePresetGenerator } from '../services/posePresetGenerator';

// Generate the 12 MVP presets using the strict logic engine
export const POSE_PRESETS_LIBRARY = PosePresetGenerator.generateAll();
